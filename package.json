{
  "name": "durham_calendar",
  "version": "0.0.1",
  "description": "A website to move your Durham timetable to Google Calendar.",
  "license": "MIT",
  "author": "Adam Carruthers (goodyguts)",
  "scripts": {
    "build": "parcel build index.html privacy_policy.html --public-url ./ --out-dir docs",
    "dev": "parcel index.html privacy_policy.html --open --out-dir docs",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "elliptic": "^6.5.4",
    "lodash": "^4.17.21",
    "luxon": "^1.28.0",
    "node-forge": ">=1.0.0",
    "noty": "^3.2.0-beta-deprecated",
    "preact": "^10.5.14",
    "uuid": "^8.3.2"
  }
}
